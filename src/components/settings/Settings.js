import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';

const Settings = () => {
  return (
    <View>
      <TouchableOpacity>
        <Text> button </Text>
      </TouchableOpacity>
    </View>
  );
};

export default Settings;
